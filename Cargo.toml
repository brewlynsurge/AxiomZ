[workspace]
members = [
    "spider",
    "shared"
]

resolver = "3"

[workspace.dependencies]
tokio = {version = "1.52.3", features = ["full"]}
serde = {version="1.0.228", features=["derive"]}
toml = "1.1.2"
crossterm = "0.29.0"

